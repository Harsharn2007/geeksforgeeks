class Solution {
public:
    int find(vector<int>& nums) {
        long long need = 0;

        for (int i = nums.size() - 1; i >= 0; i--) {
            need = (need + nums[i] + 1) / 2;
        }

        return max(1LL, need);
    }
};
